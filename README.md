# QoS — предзаказ еды без очередей

QoS — FoodTech-платформа для предзаказа еды в заведениях Алматы. Пользователь сразу попадает в каталог, выбирает ресторан, собирает заказ и указывает удобное время самовывоза.

Бонусы начисляются **не за каждый заказ**. Заведение отмечает менее загруженные временные окна, а QoS предлагает гостю бонус за предзаказ именно на такое время. Так клиент получает выгоду, а кухня распределяет поток заказов и снижает перегрузку в часы пик. В текущем MVP эта механика реализована на уровне интерфейса; серверный расчёт загрузки будет добавлен позже.

## Что есть в проекте

- Каталог из 50 реальных заведений Алматы с поиском и категориями.
- Названия, адреса, координаты, рейтинги и количество отзывов получены через Places API 2ГИС.
- Карточки заведений с бонусными временными окнами, рейтингом и временем приготовления.
- Интерактивная карта Алматы на Leaflet.
- Меню, корзина, выбор времени самовывоза и оформление заказа.
- Бонусы только при выборе отмеченного незагруженного времени.
- Адаптивный интерфейс для desktop, tablet и mobile.
- Мгновенное подключение нового заведения без модерации.
- Загрузка обложки, создание меню и предпросмотр перед публикацией.
- Бизнес-кабинет с входящими заказами, статусами, стоп-листом, загрузкой и аналитикой.
- Синхронизация статуса заказа между бизнесом и клиентским окном заказа.

## Страницы

- `/` — основной каталог заведений (открывается сразу, без лендинга).
- `/map` — тот же каталог с возможностью переключиться на карту.
- `/business` — мастер создания и мгновенной публикации заведения.
- `/business/dashboard` — рабочий кабинет заведения.

## Технологии

- Next.js 15, React 19, TypeScript
- Tailwind CSS 3
- Leaflet
- Lucide React

## Быстрый старт

Требуется Node.js 18.18+ (рекомендуется актуальная LTS-версия).

```bash
npm install
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000).

## Проверки

```bash
npm run typecheck
npm run build
```

## Данные и ограничения MVP

Данные исходных заведений сохранены в `src/data/mockVenues.ts` как локальный снимок ответа 2ГИС, чтобы каталог стабильно работал без передачи API-ключа в браузер. Созданные бизнесом заведения, меню, заказы и настройки кабинета сохраняются в `localStorage` и сразу добавляются к исходному каталогу.

Текущая реализация предназначена для автономной демонстрации на хакатоне в одном браузере. Для production потребуются серверная база данных, авторизация, облачное хранилище изображений и межустройственная синхронизация. Онлайн-оплата в текущую итерацию не входит.
